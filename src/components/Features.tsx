
import { CheckCircle, Building, Users, TrendingUp, Activity, Zap } from 'lucide-react';

const Features = () => {
  // This component has been intentionally left empty as per user request
  // to remove the "How SalesBriefGenius Works" section
  return null;
};

export default Features;
